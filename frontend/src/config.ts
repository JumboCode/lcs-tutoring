interface Config {
    backendUrl: string;
}

const config: Config = {
    backendUrl: "http://localhost:3000",
    // https://lcs-tutoring.onrender.com
    // http://localhost:3000
};

export default config;