interface Config {
    backendUrl: string;
}

const config: Config = {
    backendUrl: "https://lcs-tutoring.onrender.com",
    // https://lcs-tutoring.onrender.com
    // http://localhost:3000
};

export default config;